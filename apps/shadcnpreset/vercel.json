{
  "installCommand": "cd ../.. && pnpm install",
  "buildCommand": "cd ../.. && pnpm --filter=shadcn build && pnpm --filter=shadcnpreset exec next build",
  "crons": [
    {
      "path": "/api/internal/community-snapshot/refresh",
      "schedule": "0 3 * * *"
    }
  ]
}
